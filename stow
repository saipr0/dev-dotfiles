#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"

command -v stow >/dev/null 2>&1 || {
  echo "stow is required but was not found in PATH" >&2
  exit 1
}

stow --no-folding --target="$HOME" zsh tmux nvim mise
